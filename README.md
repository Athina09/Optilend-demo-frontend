# Optilend demo frontend

Next.js app plus **`chatbot/`**. The MSME dashboard **OptilendScore** works without the scoring API: it falls back to the same formulas in the browser if the service is down.

```bash
npm install && npm run dev
```

Copy **`.env.example`** → **`.env.local`** when using the live scoring layer or chatbot URLs.
